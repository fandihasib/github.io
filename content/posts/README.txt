ARTICLE CONTENT DIRECTORY
=========================

The active article archive is stored in data/articles.json so that the publishing
integration can update the site without a build step. This directory is reserved
for optional source notes or future long-form content. Do not move
data/articles.json unless agent/publish-config.json and the publishing integration
are updated together.
